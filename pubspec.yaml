name: count_by_clock
description: An animated widget that can show all digits by six animated clocks.
version: 0.0.3
homepage: https://github.com/meTowhid/count_by_clock


environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
